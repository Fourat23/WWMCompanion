# Database (SQLite for local dev; change for production)
DATABASE_URL="file:./dev.db"

# Salt used for hashing voter IPs (change in production!)
IP_HASH_SALT="change-this-to-a-random-string-in-production"

# Optional: set to "production" in deployment
NODE_ENV="development"
